Bootstrap: docker
From: python:3.14.3

%files
    ./ /app

%post
    pip install --upgrade pip
    pip install -r /app/requirements.txt --root-user-action

%runscript
    exec python3 /app/eve.py "$@"
